spring:
  profiles:
    active: dev
  application:
    name: providerApplication
dubbo:
  application:
    name: providerApplication
  scan:
    base-packages: com.fjh.dubbo
  registry:
    address: nacos://localhost:8848?namespace=